{
  "name": "integrated-healthcare-portal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "docker-compose up --build",
    "start": "docker-compose up --build --remove-orphans"
  }
}